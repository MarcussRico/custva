import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-poppins"
});

export const metadata: Metadata = {
  title: "Custva — Customer Retention OS for Offline Indian Businesses",
  description:
    "Custva helps cafes, restaurants, bakeries, salons, and local retailers turn walk-ins into repeat customers through WhatsApp campaigns, data capture, and retention analytics."
};

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={poppins.variable}>{children}</body>
    </html>
  );
}
